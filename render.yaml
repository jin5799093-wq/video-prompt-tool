services:
  - type: web
    name: video-prompt-tool
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: python server.py
    envVars:
      - key: GEMINI_1PSID
        sync: false
      - key: GEMINI_1PSIDTS
        sync: false
